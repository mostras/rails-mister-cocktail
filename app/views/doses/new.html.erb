<div class="new-dose">

  <h1>Set up your dose</h1>

  <%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.button :submit, id: "button" %>
  <% end %>

</div>

